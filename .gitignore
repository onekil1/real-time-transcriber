__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
data/
.env
.DS_Store
build/
dist/

# Whisper model files — большие бинарники, раздаются вне репо
models/whisper/**/*.safetensors
models/whisper/**/*.npz
models/whisper/**/*.bin
models/whisper/**/weights.*
models/whisper/**/model.bin

# Локальные установочные артефакты
start.bat
*.log
.claude/

# uv lockfile — генерируется из pyproject.toml на каждой машине,
# держать в репо нельзя (на разных платформах разные deps).
uv.lock
